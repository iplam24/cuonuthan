<template><div :class="align === 'center' ? 'text-center' : ''"><p v-if="eyebrow" class="mb-2 text-xs font-semibold uppercase tracking-editorial text-lacquer">{{ eyebrow }}</p><component :is="level" class="text-3xl font-bold leading-tight text-ink sm:text-4xl"><slot /></component><p v-if="description" class="mt-3 max-w-2xl text-sm leading-6 text-ink-muted" :class="align === 'center' ? 'mx-auto' : ''">{{ description }}</p></div></template>
<script setup>defineProps({ eyebrow: { type: String, default: '' }, description: { type: String, default: '' }, level: { type: String, default: 'h2' }, align: { type: String, default: 'left' } });</script>
